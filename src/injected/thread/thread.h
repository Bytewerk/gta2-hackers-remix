#pragma once

void injected_thread(void *param);

/*
        TODO: something like that



        typedef struct
        {
                SOCKET ...
                mem_t* mem;

        } thread_t;


        thread_t* thread_init(); // starts the thread!


        // gets called from proxy.c
        thread_cleanup(thread_t* thread);



*/
